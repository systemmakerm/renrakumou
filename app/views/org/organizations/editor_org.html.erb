<table>
  <tr>
    <td class="userflow_td1">団体編集</td>
    <td class="userflow_td2">></td>
    <td class="userflow_td3">団体編集確認</td>
    <td class="userflow_td2">></td>
    <td class="userflow_td3">団体<br />編集完了</td>
  </tr>
</table>
<h1>団体編集</h1>
<%= link_to "戻る", org_organization_path(@organization) %>
<br /><br />
<% form_for(@organization, :url => update_before_confirm_org_organizations_path(:id => @organization.id), :html => {:method => :post}) do |of| %>
  <%= of.error_messages.untaint %>
  <%= render :partial => "org_form", :locals => {:of => of} %>
※パスワードを変更される場合のみ6文字以上でご入力下さい。
  <% fields_for 'account', @organization.account do |f| -%>
    <table class="organize">
      <tr>
        <th>パスワード</th>
        <td>
          <%= f.password_field :password %>
        </td>
      </tr>
      <tr>
        <th>確認用パスワード</th>
        <td>
          <%= f.password_field :password_confirmation %>
        </td>
      </tr>
    </table>
  <% end %>
  <%= of.submit "確認" %>
<% end %>

